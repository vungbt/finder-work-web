query companyCommon {
  all_company_type(where: { isDefault: { equals: true } }) {
    data {
      ...CompanyType
    }
  }

  all_company_size(where: { isDefault: { equals: true } }) {
    data {
      ...CompanySize
    }
  }
}
